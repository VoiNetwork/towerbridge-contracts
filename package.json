{
  "name": "tower-bridge",
  "version": "1.0.0",
  "description": "",
  "main": "index.mjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "reach compile mint.rsh && reach compile lock.rsh",
    "compile-mint": "reach compile mint.rsh",
    "compile-lock": "reach compile lock.rsh",
    "deploy": "node ./index.mjs"
  },
  "author": "",
  "type": "module",
  "license": "ISC",
  "dependencies": {
    "@reach-sh/stdlib": "^0.1.13-rc.3",
    "dotenv": "^16.3.1"
  }
}
